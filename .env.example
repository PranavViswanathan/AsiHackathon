SCENARIO_DIR=./data/hackathon_data_bundle/asked_at_2025-05-29T21:00:00Z
FRONTEND_URL=http://localhost:3000
# Set AIRFLOW_WIND=1 to fetch Open-Meteo winds during `make build` (network on
# first run; cached to wind_cache.npz afterwards). Default is zero-wind + storms.
# AIRFLOW_WIND=1
